{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Go Program",
            "type": "go",
            "request": "launch",
            "mode": "debug",  // Use "debug" mode for debugging
            "program": "${workspaceFolder}/main.go",  // Adjust path if necessary
            "env": {},
            "args": []
        }
    ]
}